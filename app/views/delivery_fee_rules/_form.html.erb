<%= form_with(model: delivery_fee_rule) do |form| %>
  <% if delivery_fee_rule.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(delivery_fee_rule.errors.count, "error") %> prohibited this delivery_fee_rule from being saved:</h2>

      <ul>
        <% delivery_fee_rule.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :restaurant_id, style: "display: block" %>
    <%= form.text_field :restaurant_id %>
  </div>

  <div>
    <%= form.label :min_km, style: "display: block" %>
    <%= form.text_field :min_km %>
  </div>

  <div>
    <%= form.label :max_km, style: "display: block" %>
    <%= form.text_field :max_km %>
  </div>

  <div>
    <%= form.label :fee, style: "display: block" %>
    <%= form.text_field :fee %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
